export const cardWidth = 192 // card width in pixels

export const radius = 200 // search radius (in meters) for gmaps street view coords
export const size = `${cardWidth}x${Math.ceil((cardWidth * 2) / 3)}`
