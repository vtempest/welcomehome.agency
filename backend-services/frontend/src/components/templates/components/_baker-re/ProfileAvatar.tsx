'use client'

import { primary } from '@configs/colors'
import IcoUser from '@icons/IcoUser'

const ProfileAvatar = () => {
  return <IcoUser size={24} color={primary} />
}

export default ProfileAvatar
