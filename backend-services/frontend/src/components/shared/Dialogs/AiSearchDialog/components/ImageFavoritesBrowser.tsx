import { BrowserContainer, ImageFavoritesList } from '.'

const FavoritesBrowser = () => {
  return (
    <BrowserContainer>
      <ImageFavoritesList embedded />
    </BrowserContainer>
  )
}

export default FavoritesBrowser
